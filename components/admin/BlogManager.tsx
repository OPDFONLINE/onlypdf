"use client";
import { useState } from "react";
import { Check, Loader2, Plus, Trash2 } from "lucide-react";
type Post={id:string;title:string;slug:string;status:"draft"|"published"|"scheduled";excerpt:string|null;content:string;seo_title:string|null;seo_description:string|null;featured_image_url:string|null;category:string|null;topic_cluster:string|null;related_slugs:string[];author:string|null;published_at:string|null;scheduled_at:string|null;created_at:string;updated_at:string};
type Draft=Omit<Post,"id"|"created_at"|"updated_at"> & {id?:string};
const empty:Draft={title:"",slug:"",status:"draft",excerpt:"",content:"",seo_title:"",seo_description:"",featured_image_url:"",category:"",topic_cluster:"",related_slugs:[],author:"",published_at:null,scheduled_at:null};
export function BlogManager({initialPosts}:{initialPosts:Post[]}){const[posts,setPosts]=useState(initialPosts);const[form,setForm]=useState<Draft|null>(null);const[saving,setSaving]=useState(false);const[message,setMessage]=useState("");
function edit(post:Post){setMessage("");setForm({...post});} function update<K extends keyof Draft>(key:K,value:Draft[K]){setForm(f=>f?{...f,[key]:value}:f)}
async function save(){if(!form)return;setSaving(true);setMessage("");try{const res=await fetch("/api/admin/blog",{method:form.id?"PATCH":"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...form,related_slugs:form.related_slugs.filter(Boolean)})});const data=await res.json().catch(()=>({}));if(!res.ok)throw new Error(data.error||"Could not save article.");location.reload();}catch(e){setMessage(e instanceof Error?e.message:"Could not save article.")}finally{setSaving(false)}}
async function remove(id:string){if(!confirm("Delete this article?"))return;const res=await fetch("/api/admin/blog",{method:"DELETE",headers:{"Content-Type":"application/json"},body:JSON.stringify({id})});if(res.ok)setPosts(p=>p.filter(x=>x.id!==id));else setMessage("Could not delete article.");}
return <div className="mt-6 grid gap-6 lg:grid-cols-[minmax(0,1fr)_minmax(0,1.5fr)]"><section className="space-y-3">{posts.map(post=><div key={post.id} className={`rounded-card border-2 p-4 ${form?.id===post.id?"border-accent bg-surface":"border-border bg-surface"}`}><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-bold text-ink">{post.title||"Untitled"}</p><p className="mt-1 text-xs text-ink-soft">{post.status} · /blog/{post.slug}</p></div><div className="flex items-center gap-3"><a href={`/admin/blog/preview/${post.id}`} target="_blank" rel="noreferrer" className="text-xs font-semibold text-accent-dark">Preview</a><button onClick={()=>remove(post.id)} className="text-coral" aria-label="Delete article"><Trash2 size={16}/></button></div></div><button onClick={()=>edit(post)} className="mt-3 text-sm font-semibold text-accent-dark">Edit</button></div>)}{posts.length===0&&<p className="rounded-card border-2 border-border bg-surface p-5 text-sm text-ink-muted">No articles yet.</p>}</section>{form?<Editor form={form} update={update} save={save} saving={saving} message={message} close={()=>setForm(null)}/>:<button onClick={()=>setForm({...empty})} className="flex min-h-48 items-center justify-center rounded-card border-2 border-dashed border-border bg-surface text-sm font-semibold text-accent-dark"><Plus size={16} className="mr-2"/>Create article</button>}</div>}
function Editor({form,update,save,saving,message,close}:{form:Draft;update:<K extends keyof Draft>(k:K,v:Draft[K])=>void;save:()=>void;saving:boolean;message:string;close:()=>void}){return <section className="rounded-card border-2 border-border bg-surface p-5"><div className="flex items-center justify-between"><h2 className="text-lg font-bold text-ink">{form.id?"Edit article":"New article"}</h2><button onClick={close} className="text-sm text-ink-muted">Close</button></div><div className="mt-5 grid gap-3 sm:grid-cols-2"><Field label="Title"><input value={form.title} onChange={e=>update("title",e.target.value)} /></Field><Field label="Slug"><input value={form.slug} onChange={e=>update("slug",e.target.value)} /></Field><Field label="Category"><input value={form.category||""} onChange={e=>update("category",e.target.value)} /></Field><Field label="Topic cluster"><input value={form.topic_cluster||""} onChange={e=>update("topic_cluster",e.target.value)} /></Field><Field label="Author"><input value={form.author||""} onChange={e=>update("author",e.target.value)} /></Field><Field label="Status"><select value={form.status} onChange={e=>update("status",e.target.value as Draft["status"])}><option value="draft">Draft</option><option value="published">Published</option><option value="scheduled">Scheduled</option></select></Field><Field label="Featured image URL"><input value={form.featured_image_url||""} onChange={e=>update("featured_image_url",e.target.value)} /></Field><Field label="Scheduled at"><input value={form.scheduled_at ? form.scheduled_at.slice(0,16) : ""} onChange={e=>update("scheduled_at",e.target.value ? new Date(e.target.value).toISOString() : null)} /></Field><Field label="Excerpt" full><textarea rows={3} value={form.excerpt||""} onChange={e=>update("excerpt",e.target.value)}/></Field><Field label="SEO title"><input value={form.seo_title||""} onChange={e=>update("seo_title",e.target.value)}/></Field><Field label="SEO description"><input value={form.seo_description||""} onChange={e=>update("seo_description",e.target.value)}/></Field><Field label="Related article slugs (one per line)" full><textarea rows={3} value={(form.related_slugs||[]).join("\n")} onChange={e=>update("related_slugs",e.target.value.split(/\n+/).map(x=>x.trim()).filter(Boolean))}/></Field><Field label="Content (lightweight Markdown)" full><textarea rows={18} value={form.content} onChange={e=>update("content",e.target.value)} placeholder="# Heading\n\nWrite the article here..."/></Field></div><div className="mt-4 flex items-center gap-3"><button onClick={save} disabled={saving} className="flex items-center gap-2 rounded-pill bg-accent px-5 py-2 text-sm font-semibold text-white disabled:opacity-50">{saving?<Loader2 size={15} className="animate-spin"/>:<Check size={15}/>} {saving?"Saving…":"Save article"}</button>{message&&<span className="text-sm text-coral">{message}</span>}</div></section>}
function Field({label,children,full=false}:{label:string;children:React.ReactNode;full?:boolean}){return <label className={`text-xs font-semibold text-ink-muted ${full?"sm:col-span-2":""}`}>{label}<div className="mt-1 [&_input]:w-full [&_input]:rounded-lg [&_input]:border [&_input]:border-border [&_input]:bg-paper [&_input]:px-3 [&_input]:py-2 [&_input]:text-sm [&_input]:text-ink [&_select]:w-full [&_select]:rounded-lg [&_select]:border [&_select]:border-border [&_select]:bg-paper [&_select]:px-3 [&_select]:py-2 [&_select]:text-sm [&_select]:text-ink [&_textarea]:w-full [&_textarea]:rounded-lg [&_textarea]:border [&_textarea]:border-border [&_textarea]:bg-paper [&_textarea]:px-3 [&_textarea]:py-2 [&_textarea]:text-sm [&_textarea]:text-ink">{children}</div></label>}
