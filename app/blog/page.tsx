import type { Metadata } from "next";
import Link from "next/link";
import { getPublishedPosts } from "@/lib/blog/posts";
export const metadata:Metadata={title:"Blog",description:"PDF tips, guides, workflows, and practical document advice from OnlyPDF.",alternates:{canonical:"/blog"}};
export default async function BlogPage(){const posts=await getPublishedPosts();return <div className="container-page py-14 md:py-20"><h1 className="text-3xl sm:text-4xl">OnlyPDF Blog</h1><p className="mt-3 max-w-2xl text-ink-muted">Practical guides for working with PDFs and documents.</p><div className="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{posts.map(post=><article key={post.id} className="rounded-card border-2 border-border bg-surface p-5"><p className="text-xs font-semibold uppercase tracking-wide text-ink-soft">{post.category||"PDF guide"}</p><h2 className="mt-2 text-xl font-bold text-ink"><Link href={`/blog/${post.slug}`} className="hover:underline">{post.title}</Link></h2>{post.excerpt&&<p className="mt-2 text-sm leading-6 text-ink-muted">{post.excerpt}</p>}<Link href={`/blog/${post.slug}`} className="mt-4 inline-block text-sm font-semibold text-accent-dark">Read article →</Link></article>)}</div>{posts.length===0&&<p className="mt-10 rounded-card border-2 border-border bg-surface p-6 text-sm text-ink-muted">No published articles yet.</p>}</div>}
