import Link from "next/link";
export function renderBlogContent(markdown:string){return markdown.split(/\n{2,}/).map((block,i)=>{const text=block.trim();if(!text)return null;if(text.startsWith("### "))return <h3 key={i} className="mt-8 text-xl font-bold text-ink">{text.slice(4)}</h3>;if(text.startsWith("## "))return <h2 key={i} className="mt-10 text-2xl font-bold text-ink">{text.slice(3)}</h2>;if(text.startsWith("# "))return <h1 key={i} className="mt-10 text-3xl font-bold text-ink">{text.slice(2)}</h1>;const lines=text.split("\n");if(lines.every(l=>l.trim().startsWith("- ")))return <ul key={i} className="list-disc space-y-2 pl-6 text-ink-muted">{lines.map((l,j)=><li key={j}>{l.trim().slice(2)}</li>)}</ul>;return <p key={i} className="leading-8 text-ink-muted">{lines.map((l,j)=><span key={j}>{l}{j<lines.length-1?<br/>:null}</span>)}</p>;});}
export function RelatedLinks({slugs}:{slugs:string[]}){return slugs.length?<div className="mt-12 border-t border-border pt-8"><h2 className="text-lg font-bold text-ink">Related articles</h2><div className="mt-4 flex flex-wrap gap-2">{slugs.map(slug=><Link key={slug} href={`/blog/${slug}`} className="rounded-pill border border-border px-3 py-1.5 text-sm text-ink-muted hover:bg-surface">{slug.replaceAll("-"," ")}</Link>)}</div></div>:null;}
